# Что сделано с векторной БД (pgvector)

## ✅ Реализовано:

### 1. Установка и настройка pgvector
- ✅ Установлено расширение `vector` в PostgreSQL
- ✅ Добавлено поле `embedding vector(768)` в таблицу `analysis_history`
- ✅ Создан индекс `ivfflat` для быстрого векторного поиска

### 2. Сервис для работы с эмбеддингами (`embedding.service.ts`)
- ✅ **`generateEmbedding(text)`** — генерация эмбеддингов через Gemini API (модель `gemini-embedding-001`)
  - Размерность: 768 (обрезается с 3072 для совместимости с pgvector)
  - Обработка ошибок с fallback форматами
  - Очередь запросов для предотвращения rate limiting
  
- ✅ **`saveEmbedding(analysisHistoryId, embedding)`** — сохранение эмбеддинга в БД
  - Использует raw SQL для работы с типом `vector`
  - Преобразует массив чисел в формат PostgreSQL
  
- ✅ **`findSimilarArticles(queryEmbedding, userId, excludeId, limit, threshold)`** — поиск похожих статей
  - Использует cosine distance (`<=>`) для сравнения векторов
  - Фильтрация по пользователю (опционально)
  - Исключение текущей статьи из результатов
  - Порог схожести: 50% (настраивается)
  - Возвращает топ-N похожих статей с процентом схожести

- ✅ **`generateAndSaveEmbedding(text, analysisHistoryId)`** — комбинированная функция

### 3. Интеграция в процесс анализа
- ✅ **Автоматическое сохранение эмбеддингов:**
  - При анализе текста — эмбеддинг генерируется из текста
  - При анализе URL — эмбеддинг генерируется из `summary + reasoning + URL`
  - Сохранение происходит асинхронно после создания записи в истории
  - Ошибки генерации не прерывают основной процесс анализа

### 4. API эндпоинт для поиска похожих статей
- ✅ **`POST /api/analysis/find-similar`**
  - Принимает: `{ text, historyId?, limit? }`
  - Генерирует эмбеддинг для текста запроса
  - Ищет похожие статьи в истории пользователя
  - Возвращает список похожих статей с процентами схожести

### 5. UI для проверки в веб-приложении
- ✅ Добавлена кнопка **"🔍 Найти похожие статьи (векторная БД)"** в результатах анализа
- ✅ Отображение списка похожих статей с:
  - Ссылками на статьи
  - Процентами похожести (0-100%)
  - Краткими описаниями (summary)
- ✅ Индикация загрузки во время поиска

### 6. Обновлена модель Sequelize
- ✅ Добавлено поле `embedding` в модель `AnalysisHistory`
- ✅ Тип: `TEXT` (для Sequelize), используется как `vector` в SQL

### 7. Исправления и улучшения
- ✅ Исправлен формат API для Gemini Embedding (поддержка разных форматов ответа)
- ✅ Улучшен парсинг нескольких ссылок (каждая строка обрабатывается отдельно)
- ✅ Оптимизирован поиск (ограничение текста до 2000 символов для эмбеддинга)
- ✅ Снижен порог схожести до 50% для более широкого поиска

---

## 📊 Технические детали:

### Размерность векторов:
- **Gemini embedding-001:** 3072 измерения (по умолчанию)
- **Используется:** 768 измерений (обрезается для совместимости с pgvector)
- **Причина:** Оптимизация размера БД и производительности

### Алгоритм поиска:
- **Метрика:** Cosine distance (`<=>` оператор в PostgreSQL)
- **Формула similarity:** `1 - (embedding1 <=> embedding2)`
- **Результат:** От 0 до 1 (1 = идентичные векторы)

### Индекс:
```sql
CREATE INDEX analysis_history_embedding_idx 
ON analysis_history 
USING ivfflat (embedding vector_cosine_ops)
WITH (lists = 100);
```

---

## 🔄 Процесс работы:

1. **Пользователь анализирует статью** → создается запись в `analysis_history`
2. **Система генерирует эмбеддинг** → через Gemini API из текста статьи
3. **Эмбеддинг сохраняется** → в поле `embedding` типа `vector(768)`
4. **При поиске похожих статей:**
   - Генерируется эмбеддинг для запроса
   - Выполняется векторный поиск в БД
   - Возвращаются похожие статьи с процентами схожести

---

## 📝 SQL команды для настройки:

```sql
-- 1. Создать расширение
CREATE EXTENSION IF NOT EXISTS vector;

-- 2. Добавить поле
ALTER TABLE analysis_history 
ADD COLUMN embedding vector(768);

-- 3. Создать индекс (после добавления данных)
CREATE INDEX analysis_history_embedding_idx 
ON analysis_history 
USING ivfflat (embedding vector_cosine_ops)
WITH (lists = 100);
```

---

## ✅ Итог:

Векторная БД полностью интегрирована и работает:
- ✅ Эмбеддинги генерируются и сохраняются автоматически
- ✅ Поиск похожих статей работает через векторный поиск
- ✅ UI для проверки добавлен в веб-приложение
- ✅ Все ошибки обрабатываются корректно

Готово к использованию! 🎉
