# Проверка выполнения задач День 1 и День 2

## ✅ День 1: Подготовка базы

### 1. Проверка таблицы user_semantic_tags

**Что проверить:**
- Таблица должна создаться автоматически при запуске сервера (через Sequelize sync)

**Как проверить:**
1. Запустите сервер: `npm run dev`
2. В логах должно быть: `✅ Database models synchronized successfully.`
3. Проверьте в базе данных PostgreSQL:
```sql
SELECT * FROM user_semantic_tags LIMIT 5;
```

**Файлы:**
- ✅ `server/models/UserSemanticTag.ts` - модель создана
- ✅ `server/server.ts` - модель импортирована

### 2. Проверка эндпоинта GET /api/profile/tags

**Как проверить:**

**Вариант 1: Через curl (если есть токен)**
```bash
# Базовый запрос (все теги, отсортированные по весу)
curl -X GET http://localhost:5000/api/auth/profile/tags \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"

# С лимитом (топ-10 тегов по весу)
curl -X GET "http://localhost:5000/api/auth/profile/tags?limit=10&sortBy=weight" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"

# Сортировка по дате использования
curl -X GET "http://localhost:5000/api/auth/profile/tags?limit=20&sortBy=date" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

**Вариант 2: Через Postman или другой HTTP клиент**
- URL: `GET http://localhost:5000/api/auth/profile/tags?limit=10&sortBy=weight`
- Headers: `Authorization: Bearer YOUR_TOKEN`

**Query параметры:**
- `limit` (опционально, число от 1 до 1000) - максимальное количество тегов для возврата
- `sortBy` (опционально, 'weight' или 'date') - способ сортировки:
  - `weight` (по умолчанию) - по весу (важности) тега
  - `date` - по дате последнего использования

**Ожидаемый результат:**
```json
{
  "tags": [],
  "count": 0
}
```
(Пустой массив, если у пользователя еще нет тегов)

**После анализа статей:**
```json
{
  "tags": [
    {
      "id": 1,
      "tag": "машинное обучение",
      "weight": 2.5,
      "lastUsedAt": "2026-01-27T10:30:00.000Z",
      "createdAt": "2026-01-26T15:20:00.000Z",
      "updatedAt": "2026-01-27T10:30:00.000Z"
    },
    {
      "id": 2,
      "tag": "нейронные сети",
      "weight": 1.5,
      "lastUsedAt": "2026-01-27T09:15:00.000Z",
      "createdAt": "2026-01-27T09:15:00.000Z",
      "updatedAt": "2026-01-27T09:15:00.000Z"
    }
  ],
  "count": 2
}
```

**Файлы:**
- ✅ `server/routes/user.routes.ts` - роут добавлен
- ✅ `server/controllers/user.controller.ts` - метод getSemanticTags создан
- ✅ `server/services/user.service.ts` - метод getSemanticTags реализован

---

## ✅ День 2: AI-извлечение тем

### 1. Проверка файла semantic.service.ts

**Файл:** `server/services/semantic.service.ts`
- ✅ Файл создан
- ✅ Функция `extractThemes(text)` реализована
- ✅ Использует Gemini API
- ✅ Промпт настроен на извлечение 5-10 тем по 1-3 слова

### 2. Тестирование функции extractThemes

**Вариант 1: Через тестовый эндпоинт (без авторизации)**

```bash
curl -X POST http://localhost:5000/api/analysis/test-extract-themes \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Машинное обучение — это раздел искусственного интеллекта, который позволяет компьютерам обучаться на данных без явного программирования. Алгоритмы машинного обучения анализируют большие объемы данных, выявляют закономерности и делают прогнозы. Популярные библиотеки для машинного обучения включают TensorFlow, PyTorch и Scikit-learn. Глубокое обучение использует нейронные сети для решения сложных задач, таких как распознавание изображений и обработка естественного языка."
  }'
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "themes": [
    "машинное обучение",
    "нейронные сети",
    "глубокое обучение",
    "TensorFlow",
    "Python"
  ],
  "themesCount": 5,
  "textLength": 456
}
```

**Вариант 2: Через Postman**
- URL: `POST http://localhost:5000/api/analysis/test-extract-themes`
- Body (JSON):
```json
{
  "text": "Ваш текст статьи здесь..."
}
```

### 3. Тестирование на 3 разных статьях

**Статья 1: Про машинное обучение**
```json
{
  "text": "Машинное обучение — это раздел искусственного интеллекта, который позволяет компьютерам обучаться на данных без явного программирования. Алгоритмы машинного обучения анализируют большие объемы данных, выявляют закономерности и делают прогнозы."
}
```

**Статья 2: Про веб-разработку**
```json
{
  "text": "React — это популярная JavaScript библиотека для создания пользовательских интерфейсов. Она использует компонентный подход, что позволяет создавать переиспользуемые элементы интерфейса. React работает с виртуальным DOM для оптимизации производительности."
}
```

**Статья 3: Про здоровье**
```json
{
  "text": "Медитация и йога помогают снизить уровень стресса и улучшить общее самочувствие. Регулярные практики медитации способствуют улучшению концентрации внимания и эмоциональной стабильности. Йога сочетает физические упражнения с дыхательными техниками."
}
```

**Ожидаемые результаты:**
- Статья 1: ["машинное обучение", "искусственный интеллект", "алгоритмы", "данные"]
- Статья 2: ["React", "JavaScript", "веб-разработка", "компоненты"]
- Статья 3: ["медитация", "йога", "здоровье", "стресс"]

### 4. Проверка сохранения тегов в БД после анализа

**Важно: Темы (semantic tags) ≠ Интересы (interests)!**

**Разница:**
- **Интересы** (`user_interests`): Пользователь сам выбирает категории (например, "AI", "программирование", "спорт")
- **Теги** (`user_semantic_tags`): AI автоматически извлекает темы из проанализированных статей (например, "нейронные сети", "оптимизация моделей", "Python")

**Сценарий проверки:**

1. **Проанализируйте статью через API (с авторизацией):**
   ```bash
   curl -X POST http://localhost:5000/api/analysis/analyze \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer YOUR_TOKEN" \
     -d '{
       "urls": "https://habr.com/ru/articles/123456/",
       "interests": "AI, программирование"
     }'
   ```

2. **Проверьте, что теги сохранились в БД:**
   ```bash
   curl -X GET "http://localhost:5000/api/auth/profile/tags" \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```
   
   Должны появиться теги, извлеченные из статьи.

3. **Проверьте в PostgreSQL напрямую:**
   ```sql
   SELECT user_id, tag, weight, last_used 
   FROM user_semantic_tags 
   WHERE user_id = YOUR_USER_ID 
   ORDER BY weight DESC;
   ```

4. **Повторный анализ похожей статьи:**
   ```bash
   curl -X POST http://localhost:5000/api/analysis/analyze \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer YOUR_TOKEN" \
     -d '{
       "urls": "https://habr.com/ru/articles/789012/",
       "interests": "AI, программирование"
     }'
   ```

5. **Проверьте, что вес повторяющихся тегов увеличился:**
   ```bash
   curl -X GET "http://localhost:5000/api/auth/profile/tags" \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```
   
   Теги, которые встречались в обеих статьях, должны иметь вес больше 1.0 (например, 1.5 или 2.0).

**Ожидаемое поведение:**
- При первом анализе статьи создаются новые теги с весом 1.0
- При повторном анализе статьи с похожими темами вес существующих тегов увеличивается на 0.5 (более мягкая схема)
- `lastUsedAt` обновляется при каждом использовании тега

**Минимальная длина текста для извлечения тем:**
- `extractThemes` вызывается только для текстов длиной >= 50 символов
- Это предотвращает ненужные API-запросы для слишком коротких текстов

---

## 📋 Чек-лист для отчёта ментору

### День 1: Подготовка базы
- ✅ Создана таблица `user_semantic_tags` с полями:
  - `user_id` (INTEGER, FK к users)
  - `tag` (VARCHAR(255))
  - `weight` (DECIMAL(10,2), по умолчанию 1.0)
  - `last_used` (DATE, nullable)
- ✅ Реализован эндпоинт `GET /api/auth/profile/tags` с поддержкой query-параметров:
  - `limit` - ограничение количества тегов (1-1000)
  - `sortBy` - сортировка по весу ('weight') или дате ('date')
- ✅ Улучшен формат ответа: `{ tags: [...], count: N }`
- ✅ Модель подключена к серверу

### День 2: AI-извлечение тем
- ✅ Создан файл `server/services/semantic.service.ts`
- ✅ Реализована функция `extractThemes(text: string): Promise<string[]>`
- ✅ Функция использует Gemini API с промптом для извлечения 5-10 тем (каждая 1-3 слова)
- ✅ Добавлена проверка минимальной длины текста (50 символов) перед извлечением тем
- ✅ Улучшено логирование ошибок с указанием типов (timeout, quota, JSON parsing, network, etc.)
- ✅ Создан тестовый эндпоинт `POST /api/analysis/test-extract-themes`
- ✅ Протестировано на разных типах текстов
- ✅ Реализована функция `saveUserSemanticTags()` с улучшениями:
  - Более мягкая схема увеличения веса (+0.5 вместо +1.0)
  - Проверка и обрезка слишком длинных тегов (максимум 255 символов)
  - Логирование статистики сохранения (сколько сохранено, сколько пропущено, сколько ошибок)
- ✅ Интеграция извлечения и сохранения тегов в процесс анализа статей (URL и прямой текст)

### Что НЕ сделано (следующие этапы):
- ❌ Режимы анализа ("прочитал и понравилось" / "стоит ли читать")
- ❌ Векторная база данных для поиска похожих статей
- ❌ Визуализация "облака смыслов" на фронтенде
- ❌ Анализ Telegram-каналов

---

## 🚀 Быстрая проверка (все за раз)

1. **Запустите сервер:**
   ```bash
   cd ai-content-curator/server
   npm run dev
   ```

2. **Проверьте эндпоинт тегов (нужен токен):**
   ```bash
   # Базовый запрос
   curl -X GET http://localhost:5000/api/auth/profile/tags \
     -H "Authorization: Bearer YOUR_TOKEN"
   
   # С параметрами (топ-10 по весу)
   curl -X GET "http://localhost:5000/api/auth/profile/tags?limit=10&sortBy=weight" \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

3. **Протестируйте извлечение тем (без токена):**
   ```bash
   curl -X POST http://localhost:5000/api/analysis/test-extract-themes \
     -H "Content-Type: application/json" \
     -d '{"text": "Машинное обучение использует алгоритмы для анализа данных и создания прогнозов."}'
   ```

4. **Проверьте сохранение тегов в БД:**
   - Проанализируйте статью через `/api/analysis/analyze` (с авторизацией)
   - Проверьте теги через `/api/auth/profile/tags`
   - Повторите анализ похожей статьи и убедитесь, что вес тегов увеличился

Если все запросы работают без ошибок — всё готово! ✅
