# Суть двух режимов анализа

## 🔍 Ключевое различие

**Интересы (interests)** и **Теги (semantic tags)** — это РАЗНЫЕ вещи:

### Интересы (interests)
- **Что это:** Категории, которые ВЫ САМИ выбираете в профиле (например, "AI", "программирование", "спорт")
- **Где хранятся:** Таблица `user_interests`
- **Как используются:** Для основной оценки релевантности статьи (score 0-100)
- **Когда обновляются:** Только когда ВЫ их добавляете/удаляете вручную

### Теги (semantic tags)
- **Что это:** Темы, которые AI автоматически извлекает из проанализированных статей (например, "нейронные сети", "оптимизация моделей", "Python")
- **Где хранятся:** Таблица `user_semantic_tags`
- **Как используются:** Только для сравнения в режиме 'unread', НЕ влияют на основную оценку
- **Когда обновляются:** Автоматически при анализе статей в режиме 'read'

---

## 📖 Режим 'read' — "Я это прочитал и понравилось"

**Суть:** Вы уже прочитали статью и хотите, чтобы система запомнила, какие темы вас интересуют.

**Что происходит:**
1. AI анализирует статью и извлекает 5-10 ключевых тем
2. Эти темы сохраняются в ваше "облако смыслов" (`user_semantic_tags`)
3. Если тема уже есть — её вес увеличивается (+0.5)
4. Если тема новая — создаётся с весом 1.0

**Основная оценка релевантности (score):**
- Использует ТОЛЬКО ваши интересы (interests), которые вы указали при анализе
- Теги НЕ влияют на эту оценку

**Пример:**
- У вас интересы: "AI", "программирование"
- Вы анализируете статью про нейронные сети в режиме 'read'
- Результат: score = 85 (на основе интересов "AI", "программирование")
- Теги "нейронные сети", "глубокое обучение" сохраняются в профиль

---

## ❓ Режим 'unread' — "Стоит ли мне это читать?"

**Суть:** Вы ещё НЕ читали статью и хотите узнать, подходит ли она вам на основе того, что вы читали раньше.

**Что происходит:**
1. AI анализирует статью и извлекает 5-10 ключевых тем
2. Система получает ваши теги из "облака смыслов" (`user_semantic_tags`)
3. Сравнивает темы статьи с вашими тегами
4. Показывает процент совпадения и списки совпавших/новых тем
5. **Теги НЕ сохраняются** в профиль

**Основная оценка релевантности (score):**
- Использует ТОЛЬКО ваши интересы (interests), которые вы указали при анализе
- Теги НЕ влияют на эту оценку
- Результат сравнения тегов показывается отдельно в `semanticComparison`

**Пример:**
- У вас интересы: "AI", "программирование"
- У вас есть теги в профиле: "нейронные сети" (вес 2.5), "машинное обучение" (вес 1.5)
- Вы анализируете статью про трансформеры в режиме 'unread'
- Результат: 
  - score = 80 (на основе интересов "AI", "программирование")
  - semanticComparison: 60% совпадение (тема "нейронные сети" совпала)
  - Теги НЕ сохранились

---

## ⚠️ ВАЖНО: Почему оценка низкая при удаленных интересах?

**Проблема:** Вы удалили интересы "AI", "программирование", но в "облаке смыслов" есть теги на эту тему. Статья оценивается низко.

**Причина:**
- Основная оценка релевантности (score 0-100) использует ТОЛЬКО интересы (interests)
- Теги (semantic tags) используются ТОЛЬКО для сравнения в режиме 'unread'
- Если вы удалили интересы, система не знает, что вас интересует эта тема
- Теги в "облаке смыслов" НЕ влияют на основную оценку

**Решение:**
- Если хотите, чтобы статьи по AI оценивались высоко — добавьте обратно интерес "AI" в профиль
- Теги в "облаке смыслов" — это дополнительная информация для режима 'unread', но не основа для оценки

---

## 📊 Итоговая таблица

| Параметр | Режим 'read' | Режим 'unread' |
|----------|--------------|----------------|
| **Основная оценка (score)** | Использует интересы | Использует интересы |
| **Теги сохраняются?** | ✅ Да | ❌ Нет |
| **Сравнение тегов** | ❌ Нет | ✅ Да (semanticComparison) |
| **Когда использовать** | После прочтения статьи | Перед прочтением статьи |

---

## 💡 Рекомендации

1. **Для накопления "облака смыслов":** Используйте режим 'read' для статей, которые вам понравились
2. **Для рекомендаций:** Используйте режим 'unread' для новых статей, чтобы увидеть, насколько они соответствуют вашим интересам
3. **Для высокой оценки:** Всегда указывайте актуальные интересы при анализе — они влияют на score
4. **Теги vs Интересы:** Интересы — это то, что вы хотите изучать. Теги — это то, что вы уже изучили
