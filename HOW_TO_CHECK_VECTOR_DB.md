# Как проверить работу векторной БД в приложении

## Что было исправлено:

### 1. Ошибка с форматом API для эмбеддингов
- **Проблема:** "Value must be a list given an array path requests[]"
- **Исправление:** Использован правильный формат для Gemini Embedding API (`contents` как строка)
- **Результат:** Эмбеддинги теперь генерируются корректно

### 2. Обработка нескольких ссылок
- **Проблема:** Две ссылки на разных строках обрабатывались как один текст
- **Исправление:** Улучшен парсинг - каждая строка проверяется отдельно на валидность URL
- **Результат:** Каждая ссылка обрабатывается отдельно

### 3. Улучшена логика поиска похожих статей
- Снижен порог схожести с 70% до 50% для более широкого поиска
- Улучшена обработка результатов (нормализация similarity)
- Добавлено ограничение текста для эмбеддинга (2000 символов) для производительности

---

## Как проверить в приложении:

### Шаг 1: Проверка обработки нескольких ссылок

1. Войдите в систему
2. Перейдите в раздел "Анализ"
3. Вставьте **две ссылки на разных строках**:
   ```
   https://habr.com/ru/articles/123456/
   https://habr.com/ru/articles/789012/
   ```
4. Нажмите "Анализировать"
5. **Ожидаемый результат:** Должны появиться **два отдельных результата анализа**, каждый со своим ID

### Шаг 2: Проверка сохранения эмбеддингов

1. После анализа статьи проверьте логи сервера:
   ```
   ✅ Generated and saved embedding for analysis_history ID: [номер]
   ✅ [saveEmbedding] Saved embedding for analysis_history ID: [номер]
   ```
2. Если видите эти сообщения - эмбеддинги сохраняются ✅

### Шаг 3: Проверка поиска похожих статей

1. Проанализируйте **2-3 статьи на похожие темы** (например, про нейронные сети)
2. Проанализируйте еще одну статью на похожую тему
3. В результатах анализа нажмите кнопку:
   ```
   🔍 Найти похожие статьи (векторная БД)
   ```
4. **Ожидаемый результат:**
   - Появится список похожих статей из вашей истории
   - Каждая статья будет с процентом похожести (например, "85% похоже")
   - Статьи отсортированы по похожести (самые похожие сверху)

### Шаг 4: Проверка через SQL (опционально)

Если хотите проверить напрямую в БД:

```sql
-- Проверить записи с эмбеддингами
SELECT 
    id, 
    url, 
    CASE 
        WHEN embedding IS NOT NULL THEN 'Есть' 
        ELSE 'Нет' 
    END as has_embedding
FROM analysis_history
ORDER BY id DESC
LIMIT 10;

-- Проверить количество записей с эмбеддингами
SELECT 
    COUNT(*) as total,
    COUNT(embedding) as with_embedding
FROM analysis_history;
```

---

## Что должно работать:

✅ **Обработка нескольких ссылок:** Каждая ссылка на новой строке обрабатывается отдельно  
✅ **Сохранение эмбеддингов:** Автоматически при анализе статей  
✅ **Поиск похожих статей:** Кнопка в результатах анализа находит похожие статьи  
✅ **Извлечение тегов:** Теги извлекаются корректно (1-3 слова, преимущественно 1-2)  
✅ **Сравнение тегов:** Режим 'unread' корректно сравнивает темы статьи с тегами пользователя  

---

## Возможные проблемы:

### Проблема: "Похожие статьи не найдены"
**Причины:**
- В истории меньше 2 статей
- Статьи на совершенно разные темы
- Эмбеддинги еще не сгенерированы (подождите несколько секунд)

**Решение:** Проанализируйте еще несколько статей на похожие темы

### Проблема: Ошибка при генерации эмбеддинга
**Проверьте:**
- Логи сервера на ошибки API
- Что `GEMINI_API_KEY` установлен
- Что расширение `vector` установлено в PostgreSQL

---

## Итог:

После всех исправлений:
1. ✅ Несколько ссылок обрабатываются отдельно
2. ✅ Эмбеддинги генерируются и сохраняются корректно
3. ✅ Поиск похожих статей работает через векторную БД
4. ✅ Теги извлекаются правильно
5. ✅ Сравнение тегов работает логично

Всё готово к использованию! 🎉
