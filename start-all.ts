// Скрипт для запуска сервера и бота вместе
import './server';

// Ждем немного, чтобы сервер успел запуститься, затем запускаем бота
setTimeout(() => {
    console.log('🤖 Starting Telegram bot after server initialization...');
    require('./bot-runner');
}, 3000); // Даем 3 секунды на запуск сервера

