# Реализация RAG (Retrieval-Augmented Generation)

## ✅ Что было реализовано

RAG интегрирован в систему для улучшения анализа статей и рекомендаций за счет использования истории пользователя.

### 1. RAG в анализе статей (`analyzeContent`)

**Файл:** `server/services/ai.service.ts`

**Что делает:**
- При анализе статьи автоматически находит похожие статьи из истории пользователя (векторный поиск)
- Использует похожие статьи как контекст для AI
- AI учитывает историю при оценке релевантности и генерации саммари

**Как работает:**
1. Генерируется эмбеддинг текущей статьи (первые 2500 символов)
2. Находится топ-5 похожих статей из истории пользователя (порог схожести 65%+)
3. Похожие статьи добавляются в промпт как контекст
4. AI анализирует статью с учетом похожих статей из истории

**Пример контекста:**
```
📚 КОНТЕКСТ ИЗ ВАШЕЙ ИСТОРИИ (похожие статьи, которые вы анализировали ранее):
1. URL: https://habr.com/...
   Похожесть: 85%
   Саммари: Статья про React hooks...

2. URL: https://habr.com/...
   Похожесть: 78%
   Саммари: Статья про оптимизацию производительности...
```

**Преимущества:**
- Более точная оценка релевантности (AI видит паттерны из истории)
- Улучшенные саммари (AI может сравнивать с похожими статьями)
- Учет предпочтений пользователя (если похожие статьи были оценены высоко)

### 2. RAG в рекомендациях (`generateSemanticRecommendation`)

**Файл:** `server/services/semantic.service.ts`

**Что делает:**
- При генерации рекомендации в режиме "unread" использует похожие статьи как контекст
- Рекомендация учитывает не только теги, но и реальные статьи из истории

**Как работает:**
1. Генерируется эмбеддинг статьи
2. Находится топ-3 похожие статьи (порог 65%+)
3. Похожие статьи добавляются в промпт для рекомендации
4. AI генерирует рекомендацию с учетом похожих статей

**Пример улучшенной рекомендации:**
```
Эта статья похожа на "Статья про React hooks" (85% похоже), 
которую вы читали ранее. Рекомендуется к прочтению, так как 
развивает темы, которые вам интересны.
```

### 3. Graceful Degradation (отказоустойчивость)

**Важно:** RAG работает опционально и не блокирует основной функционал:

- ✅ Если RAG не работает (нет userId, ошибка эмбеддинга, нет похожих статей) → анализ продолжается без RAG
- ✅ Если похожих статей нет → анализ работает как обычно
- ✅ Все ошибки RAG логируются, но не прерывают основной процесс
- ✅ Система работает даже если векторная БД недоступна

## 📊 Технические детали

### Функции RAG

1. **`getRAGContext(content, userId?, interests?)`** в `ai.service.ts`
   - Генерирует эмбеддинг статьи
   - Находит похожие статьи из истории пользователя
   - Формирует контекст для промпта

2. **`getRAGContextForRecommendation(articleText, userId)`** в `semantic.service.ts`
   - Аналогично, но для рекомендаций (топ-3 статьи)

### Параметры

- **Порог схожести:** 65% (0.65)
- **Количество статей для анализа:** 5
- **Количество статей для рекомендаций:** 3
- **Длина текста для эмбеддинга:** 2500 символов

### Интеграция

**Изменения в коде:**

1. `analyzeContent` теперь принимает `userId?` параметр
2. `generateSemanticRecommendation` теперь принимает `articleText?` и `userId?`
3. Контроллеры передают `userId` в функции анализа
4. RAG контекст добавляется в промпты автоматически

## 🎯 Результаты

### До RAG:
- AI анализировал каждую статью изолированно
- Рекомендации основывались только на тегах
- Не учитывалась история пользователя

### После RAG:
- ✅ AI видит похожие статьи из истории
- ✅ Рекомендации учитывают реальные статьи, которые пользователь читал
- ✅ Более точная оценка релевантности
- ✅ Улучшенные саммари с учетом контекста

## 🔍 Логирование

RAG логирует свои действия:

```
🔍 [RAG] Generating embedding for RAG context (2500 chars)...
✅ [RAG] Found 3 similar articles for RAG context
✅ [RAG] RAG context generated successfully (1234 chars)
```

Если RAG не работает:
```
⚠️ [RAG] Failed to generate RAG context: Error message
⚠️ [RAG] Failed to get RAG context, continuing without it: Error message
```

## 📝 Примеры использования

### Анализ статьи с RAG:

```typescript
// Автоматически используется RAG если userId передан
const result = await analyzeContent(
    content,
    interests,
    feedbackHistory,
    url,
    userId // RAG будет использован
);
```

### Рекомендация с RAG:

```typescript
// Автоматически используется RAG если articleText и userId переданы
const recommendation = await generateSemanticRecommendation(
    themes,
    userTags,
    comparisonResult,
    articleText, // Для RAG
    userId // Для RAG
);
```

## ✅ Проверка работы

1. **Проанализируйте несколько статей** в режиме "Я это прочитал и понравилось"
2. **Проанализируйте похожую статью** - в логах должно появиться:
   ```
   🔍 [RAG] Generating embedding for RAG context...
   ✅ [RAG] Found X similar articles for RAG context
   ```
3. **Проверьте рекомендации** в режиме "unread" - они должны учитывать похожие статьи

## 🚀 Будущие улучшения

- [ ] Кэширование RAG контекста для одинаковых статей
- [ ] Настройка порога схожести через конфиг
- [ ] Использование оценок из истории для улучшения рекомендаций
- [ ] RAG для чат-бота с историей пользователя
